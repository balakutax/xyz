@model RJ.Tokoku.DataLayer.Products.ProductGroup

@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Details</h2>
@if (ViewBag.IsDelete == true)
{
    <div>Are you sure want to delete this data?</div>
}
    <div>
        <h4>ProductGroup</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.ProductGroupCode)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.ProductGroupCode)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Description)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Description)
            </dd>
        </dl>
    </div>
    @if (ViewBag.IsDelete == true)
    {
        <div>

            @using (Html.BeginForm("Delete", "ProductGroup"))
            {
                @Html.AntiForgeryToken()
                @Html.HiddenFor(x => x.ProductGroupCode)
                <input type="submit" value="Delete" class="btn btn-default" />
            }
        </div>
    }
    <p>
        @Html.ActionLink("Edit", "Edit", new { productGroupCode = Model.ProductGroupCode }) |
        @Html.ActionLink("Back to List", "Index")
    </p>
